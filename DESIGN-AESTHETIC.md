# DESIGN-AESTHETIC.md — playground 介面的美學方向（先立規範，尚未實作）

> **狀態：規範文件，不是實作指令。** 現有介面維持原狀；
> 之後任何新頁面、改版、或元件重刷，設計決策照這份走。
>
> **權威來源是官網 repo**：`../website/CLAUDE.md`（不可違反的規則）與
> `../website/DESIGN.md`（完整論證）。本檔是翻譯給 playground 情境的摘要 ——
> 兩邊矛盾時以官網那兩份為準，並回來修這份。

## 現況與落差

`static/css/style.css` 與 `static/css/canvas.css` 目前仍是**阿里雲 demo 的配色**
（這個 repo fork 自 `alibaba-model-demo`）：

| 現況 | 問題 |
|---|---|
| `--ali-orange: #01A0C7`、`--ali-blue: #0064C8` | 兩個彩度，且都不是本家色 |
| `--bg: #F5F6F8`、`--text-h: #1A1A2E` | 冷灰中性階，與暖白紙面是不同世界 |
| `--green: #00B365`、`--red: #F53F3F` | 純飽和狀態色，在紙面上會過亮 |

playground 是內部測試台，**不急著重刷**。但它會被拿來錄示範、截圖、對外展示，
那時視覺語言不該跟官網與控制台斷裂。

## 目標色料（與官網同名同值）

| Token | 值 | 用途 |
|---|---|---|
| `--ground` | `#FBFAF8` | 暖白紙面（頁面底色） |
| `--ink` | `#2B2724` | 標題與主文字（14:1） |
| `--body` | `#5C564F` | 正文（6.9:1） |
| `--rain` | `#787168` | 次要文字（4.6:1，過 AA 的最淡值，不可再淡） |
| `--line` | `#E6E2DB` | 髮絲線（分隔、邊框） |
| `--flash` | `#7FA39E` | 汝窯天青——**裝飾與圖表的唯一彩度** |
| `--flash-ink` | `#3F625F` | 天青的文字階（6.4:1） |
| `--ember` | `#8C4A3F` | 釉裡紅，**只給狀態語意**（錯誤／告警／破壞性動作），6.36:1 |
| `--ground-2` | `#F6F2E9` | 第二層紙（比底色深四階，做層次不做色塊） |
| `--r` | `10px` | 唯一的圓角 |

深色模式：`--ground` → `#191715`、`--ink` → `#EDEAE4`、`--flash` → `#9CC0BA`、
`--flash-ink` → `#A9CCC6`、`--ember` → `#E8B9AC`。

### 狀態色怎麼對應現有的綠與紅

- **`--red: #F53F3F` → `--ember`**（釉裡紅）。彩度壓到最低，
  與天青文字階同一個對比階，不是刺眼的純紅。
- **`--green: #00B365` → `--flash-ink`**（天青文字階）。
  天青本來就是低彩度的青綠 —— 要的「成功／健康」它已經是了，不必再引入一個。
- 語意色**只做語意**：狀態點、錯誤訊息、驗證提示。
  不做裝飾、不當大面積底色、不進圖表配色。
- **狀態不可以只靠顏色傳達**（WCAG）：配一個 icon、一道界線或文字標籤。

## 紀律

- **沒有深色底**（深色模式除外，那是整站切換）。
- **不用實心色塊表達選中** —— 用字色與髮絲線。
  按鈕同理：主要動作可以用天青底，但一個畫面只有一個。
- **留白與髮絲線做結構，不用卡片堆疊**，陰影不當層次。
- 圓角只有一階（10px），不混用。

## 字型

- 標題（頁面級、區塊級）：明體（`Noto Serif TC` + `Spectral`）。
- 正文與 UI 控制項：黑體（系統 `PingFang TC` + `Archivo`）。
- **兩層不可交叉**；**只有 400 一階字重**，層級靠字級與留白。
- 數字、模型代號、token 量、JSON：`JetBrains Mono`，**只給拉丁與數字**，
  中文不進 mono（它沒有中文字符集）。
- 高密度的測試結果區可以全部用黑體 —— 密度優先於儀式感。

## AI Canvas 特有

`/canvas` 用 litegraph.js，它自帶一套深色節點樣式。這是**目前唯一有正當理由
用深色的地方**（節點式編輯器的慣例，且官網的「AI Canvas」示意圖本身就是
在畫深色畫布上的淺色卡片）。若要調整：

- 節點的連線與 port 用天青一階，不要彩虹色分類。
- 錯誤狀態的節點用釉裡紅描邊，不要純紅填色。
- 節點卡片維持淺色紙面，深色只出現在畫布背景。

## 明確拒絕的（與官網同一張清單）

近黑底（canvas 畫布除外）、霓虹漸層、多色相圖表、發光效果、
實心色塊當選中態、卡片陰影當結構、超過一階的字重堆疊、emoji 當圖示。

## 之後實作時的順序建議（現在不動）

1. 先把色料表落成 CSS variables 的一次性覆寫（`style.css` 的 `:root`），
   把 `--ali-*` 對映到新 token，不動任何版面。
2. 新頁面直接照本規範寫。
3. 既有頁面等有功能改版需求時順路刷，不專門為美學重做。
