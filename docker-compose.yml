services:
  ai-model-tester:
    build: .
    ports:
      - "80:5050"
    volumes:
      - ./outputs:/app/outputs
      - ./static/uploads:/app/static/uploads
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
