# memory.md — 三個專案的關係與跨專案協作備忘

這份文件記錄「測試平台（本專案）／閘道／文檔站」三個 repo 之間的關係、各自的職責邊界，以及在這條鏈上反覆踩過的坑。**目的是讓新接手的人不必重新踩一次**——底下每一條「教訓」都是實際造成過錯誤判斷或錯誤設定的。

日常開發規範看 `CLAUDE.md`，逐次變更紀錄看 `update.md`，模型清單與參數細節看 `README.md`。

---

## 一、三個專案

### 1. 測試平台（本專案）

- 路徑：`/Users/levi/claude_code/nen_ai_project/nenai-playground`
- 遠端：`github.com/levilan/alibaba-model-demo`，開發分支 `nenai`
- 技術：FastAPI（單檔 `app.py`）＋ 原生 JS，無資料庫、無 build
- 正式站：**https://playground.nen.com.tw**
- 角色：**這條鏈的最末端**。只負責「把參數用上游看得懂的格式送出去、把結果呈現給人看」，不做模型邏輯、不做計費。

### 2. 閘道 nen-ai-platform

- 路徑：`/Users/levi/claude_code/nen_ai_project/nen-ai-platform`
- 遠端：`github.com/HigherCloudMaster/highercloud-ai-platform`，工作分支 `feat/carrothub-channel-support`
- 技術：Go，「New API」那一系的多渠道 AI 閘道
- 正式站：**https://nen.com.tw**（`NENAI_BASE` 預設值）；測試網關 `http://192.168.0.245`
- 角色：**我們與各家原廠之間唯一的中介**。負責渠道路由、請求格式轉換、輪詢非同步任務、計費。

我們送出的任何參數，能不能生效**完全取決於這一層有沒有映射它**。這是本專案絕大多數「參數送了卻沒作用」問題的根源。

### 3. 文檔站 Nen-AI-Docs-V1

- 路徑：`/Users/levi/claude_code/nen_ai_project/Nen-AI-Docs-V1`
- 遠端：`github.com/HigherCloudMaster/Nen-AI-Docs-V1`，分支 `main`
- 角色：對外的 API 文檔站。

### 資料流

```
瀏覽器 → 測試平台 (Cloud Run) → nen 閘道 → 各家原廠 (DashScope / Google / OpenAI / 智譜 / 字節…)
                                    ↓
                              文檔站（描述上面這一切）
```

---

## 二、職責邊界（不要越界）

- **本專案不改閘道的程式碼。** 發現閘道有問題，是回報給 `nen-ai-platform` 的 session，由他們判斷與修改。
- **本專案不改文檔站的檔案。** 新增模型後把資訊送過去，由文檔站的 session 撰寫。
- 三個 repo 各有自己的 Claude session。跨專案溝通用 `ListAgents` 找對象、`SendMessage` 送訊息，**不要直接跑到別人的目錄改檔案**。

`CLAUDE.md` 已把「新增模型後通知文檔 session」定為必做流程；找不到 session 時要**明確提醒使用者啟動**，不要默默略過。

---

## 三、基礎架構（2026-08-11 實查）

GCP 專案 `ai-model-hub-newapi`，**測試平台與閘道都在 `us-east5`**。

| | 測試平台 | 閘道 |
|---|---|---|
| LB IP | `34.128.190.102` | `34.54.144.102` |
| 網路層級 | `PREMIUM`（全球 anycast） | `PREMIUM` |
| 運算 | Cloud Run `nenai-testing-platform`（us-east5，`minScale` 未設＝會冷啟動） | VM instance group（us-east5-c） |
| LB 後端逾時 | 30s（**對 serverless NEG 不生效**，實際受 Cloud Run `timeoutSeconds: 300` 管轄） | 1800s |

**效能結論：平台幾乎不增加延遲。** 同一個 2048×2048 請求，透過平台 44.8s、直接打閘道 44.0s，**只差 0.8 秒**（兩者同區域）。若有人反映「透過平台比較慢」，先確認是不是在比較不同的工作負載——實測 i2i 約 11s、t2i 約 24s，**編輯本來就比文生圖快一倍以上**；而且同一請求的變異可達 40%（26.8s vs 38.5s），單次測量不可靠。

---

## 四、反覆踩過的坑（最重要的部分）

### 1. 「通過驗證」不等於「能用」

閘道的參數驗證比原廠寬鬆，會放行原廠實際不支援的組合：

- `wan2.7-image` 送 `4096*4096`、pro 開組圖送 `4K`，**閘道都放行**，但官方文件明說不支援（閘道只檢查總像素落在最寬鬆的那組範圍）。
- `dreamina-seedance-2.0` @1080P 提交回 200，任務跑 15 分鐘後 `failed`。

**所以「打閘道試得通」不能當作支援的證據。** 尺寸／能力清單以官方文件為準，能端到端產出成品再量測才算驗證。

### 2. 不要從閘道的 Go struct 推斷行為

`WanImageInput.images` 宣告上限 2 張，我據此把萬相編輯的參考圖上限設成 2——**實測是 9 張都生效**（那條約束不適用於 `/v1/images/edits` 這條路徑）。這個錯誤限制正好對應使用者反映的「wan2.7 只能傳兩張，阿里可以傳九張」。

Go struct 描述的是某一條路徑的資料結構，不等於所有路徑的行為。**要驗就實際打。**

這其實是下面 4d 第④類「**拿間接證據當行為證據**」的一個特例——從描述系統的東西（struct、`/v1/models` 列表、metadata）去推系統的行為，而不是送一次請求。文檔站的 Veo 那次是同一形狀。認出這個形狀比記住個案有用：**只要證據不是「我送了一次請求、看到這個結果」，它就是間接的。**

### 3. 探測手法不能跨模型家族沿用

「送超範圍的 `n` 當哨兵、靠錯誤訊息判斷 size 是否合法」在萬相家族有效（size 先驗），但**千問 3.0 的 `n` 先驗**，導致連 `10*10` 都回「n 的錯誤」、看起來像尺寸通過了。差點據此把一堆沒驗證過的尺寸寫進清單。

而且這招對 MAI／Seedream **會真的產圖**（它們不拒絕 `n=13`），曾因此一次意外生成上百張圖。用這招前**務必先確認該模型真的會拒絕哨兵參數**，而且保護機制不能在重寫腳本時被拿掉。

### 4. 官方文件會過時、會寫錯

- 千問 3.0 的 `usage` 官方文件寫 `{width, height, image_count}`，**實際是** `output_width` / `output_height` / `output_image_count` / `output_image_type`。
- 千問 3.0 文件說「目前可用的是 pro」，但 base 版實際可用。
- `wan3.0-video` 的 API 文檔至今是邀請制未公開。

### 4b. 分清楚「觀察到的數據」與「從數據推出的因果」

標明「實測」還不夠——實測的是**數據**，從數據推出的**因果**仍然是推論，要分開講。

實例：我看到 `qwen-image-3.0` `n=2` 回 `usage.output_image_count: 2` 而 `data[]` 只有 1 筆（數據，正確），就宣稱「使用者付了 2 張的錢卻只拿到 1 張」（因果，**錯的**）。閘道實際是**數 `data[]` 的圖片數**計費，使用者只被收 1 張——真正吃虧的是平台方。這個錯誤結論已經流進文檔站才被閘道端查用量日誌打回。

跨專案交接時，建議分三層寫：**我實測到的數據** / **我從中推出的結論** / **我轉述自他人的**。

### 4c. 要知道「實際收多少錢」，用用量增幅量，不要推

`GET https://nen.com.tw/v1/dashboard/billing/usage`（帶一般的 `Authorization: Bearer`）回傳這把 key 的累計用量：

```json
{"object":"list","total_usage":155615.3236}
```

做法是**前後各查一次、比對增幅**：先取基準，送出要測的請求，等一下再取一次。平台開了 `BATCH_UPDATE_ENABLED`，用量不是即時刷寫的，**要等約 12 秒**才反映得出來。

這是唯一能直接回答「這個請求實際被收多少」的方法。`usage.output_image_count`、`model_price` 這些都只是線索，不是計費依據——上面 4b 那個錯誤就是把線索當結論。

實例：文檔站用這個方法驗證 `qwen-image-3.0` 的 `n=1` 與 `n=2` 增幅都是 `+3.0`（比值 1.0），獨立確認了「計費依 `data[]` 實際張數」。

### 4d. 一次取樣的數字不能當結論——尤其別讓它流進客戶文檔

模型的 token 用量本身就會浮動。實測 `grok-4.3` 同一題、同一個 `reasoning_effort` 連跑 5 次，`low` 檔的推理 token 落在 **205～353**（同檔內差距 1.7 倍）。所以任何「某檔大約幾個 token」的單次觀測都可能只是雜訊。

實例：我把每檔各跑一次的結果（`minimal` 304 / `low` 314 / `medium` 668 / `high` 362）送給文檔站，對方據此寫進客戶文檔。重跑 5 次後：

| effort | 5 次的推理 token | 中位數 |
|---|---|---|
| `none` | 0, 0, 0, 0, 0 | 0 |
| `minimal` | 282, 292, 339, 379, 334 | 334 |
| `low` | 205, 353, 272, 232, 277 | 272 |
| `medium` | 359, 528, 527, 615, 608 | 528 |
| `high` | 265, 418, 489, 366, 336 | 366 |

**結論恰好是對的**（`medium` 的整個範圍都高於 `high`，反轉可重現 → `reasoning_effort` 是「傾向」不是預算上限），**但當初的證據不足以支撐它**。而 `minimal`/`low`/`high` 三檔區間大幅重疊，原本那組單次數字看起來有穩定差距，其實根本分不出來。

**⚠️ 原本這裡寫的判準是錯的，而且我在寫下它之後當天就再犯一次。** 原判準是「『有沒有效果』單次夠用（0 vs 非 0 是離散的），『效果多大』才要重複取樣」。

打臉的實例：`gemini-3.5-flash-lite` 的 `thinkingBudget`。單次取樣看到 `128` → 沒有 `thoughtsTokenCount`、`512` → 有，於是我寫下「128 被靜默忽略、實務門檻約 512」，還特地叮嚀文檔站「寫成觀察句、別寫成規格斷言」——**觀察句本身就是錯的**。每格改跑 8 次：

| `thinkingBudget` | 8 次裡有思考的次數 |
|---|---|
| ≤96 | 0/8（一律不思考，且不報錯） |
| 128 | 4/8 |
| 512 | 3/8 |
| 600 | 3/8 |
| −1 | 8/8 |

真相是：門檻在 96～128 之間，**超過門檻後思考與否逐次隨機**（約四成），**跟預算大小無關**。沒有 512 這個門檻，那只是 128 與 512 各抽一次、剛好一個沒中一個中。

所以正確的判準是：**「有沒有效果」單次只有在觀測到「有」的時候才算數。**

- 看到**有**效果 → 一次就夠（能做到就是能做到，不會是運氣）
- 看到**沒有**效果 → **一次完全不夠**。「沒觀測到」和「不存在」是兩回事，可能只是這次沒觸發
- 「效果多大／誰比誰大」 → 一律重複取樣並附範圍

換句話說，**否定的結論比肯定的結論需要更多樣本**，而我們最常犯的錯正是拿單次的「沒看到」去斷言「不支援／被忽略／有門檻」。要送出去給別人採信的數字，附上樣本數與範圍，不要只給一個點。

**另一個維度：樣本夠多也不夠——要挑「能分辨假設」的測試值。** 樣本數解決的是雜訊，解決不了「這個資料點同時符合好幾種假設」。

實例（📄 轉述自 `nen-ai-platform` session，2026-08-12）：MAI 上游會把尺寸對齊，實測 `1366 → 1360`。這一個資料點**同時符合三種規則**：

| 假設 | 對 1366 的預測 |
|---|---|
| 往下取 16 的倍數（floor-16） | 1360 ✔ |
| 往下取 8 的倍數（floor-8） | 1360 ✔ |
| 四捨五入到 16 的倍數（nearest-16） | 1360 ✔ |

再多跑幾次 `1366` 也永遠分不出來。他們改送 `1372`：floor-16 預測 1360、floor-8 預測 1368、nearest-16 預測 1376——**三個預測互不相同**，一次呼叫就定案（實際 1360，確認 floor-16）。

所以歸納規則時的順序是：**先寫下所有還說得通的假設，再挑一個能讓它們預測分歧的輸入**。挑不出分歧的輸入，就代表這次測試不管跑幾次都不會增加資訊。這比「多跑幾次」省錢也省時間——尤其在每次呼叫都計費的情境。

**第三個維度：樣本數解決雜訊，解決不了「變因沒被控制」。** 同一個測試跑 8 次很紮實，但如果只用一組輸入，就會把「這組輸入的行為」寫成「這個系統的屬性」。

實例（同一張 `thinkingBudget` 表被改了兩次，兩次都是取樣方式的問題）：

1. 每格取樣一次 → 寫成「128 靜默忽略、門檻約 512」。**次數不足**，湊出一個不存在的門檻。
2. 每格 8 次 → 寫成「超過門檻後逐次隨機、機率約四成、跟預算無關」。次數夠了，但**只用了一個 prompt**。文檔站用另一個 prompt 得到 8/8，跟我的 4/8 直接衝突。
3. 同一輪背對背跑兩種 prompt，才定位到真正的變因：**是 prompt 決定模型要不要思考**，不是預算大小。需多步算術的題目 8/8、短句陷阱題 3～4/8，兩個 prompt 在每個 budget 上都重現。

分歧出現時的處理順序也在這裡驗證過一次：對方沒有說「你錯了」，而是**把完整的 request body 貼過來要我比對**。我把兩組 body 放進同一輪執行，一次就分辨出「請求不等價」與「上游改版」這兩個假設——這正是上面「挑能分辨假設的輸入」的應用，只是這次分歧的維度是輸入本身，不是參數值。

**第三個實例（2026-08-14，`gemini-3.7-flash` 的 `thinkingBudget: 0`）——同樣的錯又犯一次。** 我用短句陷阱題測了每格 5 次，看到 `budget=0`（86～158）與不帶設定（139～170）重疊，就寫下「思考關不掉、靜默忽略」並據此**不給使用者開關**。閘道端 session 用另一組題目得到完全不重疊的結果，重測才發現：

| 題目類型 | `budget=0` | 不帶設定 | |
|---|---|---|---|
| 多步算術 | 76～94 | 180～294 | **完全不重疊** |
| 短句陷阱題 | 0～158 | 121～183 | 重疊 |

**陷阱題的基準思考量本來就低，沒有下降空間**——不是參數沒用，是我挑的題目量不出它。這次的代價是差點讓一個能省六成 thinking token 的開關永遠不出現在 UI 上。

判準：**下結論前先問「我換一組輸入，這個結論還成立嗎？」** 如果沒試過，那個結論的適用範圍就只到你測過的那組輸入。

**對「參數有沒有用」這類問題，還要再多問一句：我的測試情境有讓這個參數發揮的空間嗎？** 測「降低思考」就要用會大量思考的題目，測「提高上限」就要用會撞到上限的輸入。拿一個本來就不觸發該行為的情境去測，看到「沒差別」是必然的，跟參數有沒有用無關。

這條同樣適用於**輸入**，不只參數。閘道端 session 提供的同型實例（📄 轉述）：他們用 **1×1 的 PNG** 去測 kimi 的 data URI 圖片輸入，拿到失敗訊號就推論「不支援 Base64」——但 1×1 的圖連讓模型看見內容的空間都沒有，**那個訊號本來就沒有診斷價值**。換成 64×64 純色圖就 3/3 成功。

所以事前檢查有兩個方向，兩個都要問：

- **參數**：這個情境有讓它發揮的空間嗎？（測降低就要有東西可降）
- **輸入**：這個輸入有讓系統產生可判讀訊號的空間嗎？（測看圖就要圖裡真的有東西可看）

⚠️ 而且**這兩種錯誤都只會產生「否定」結論**——「沒差別」「不支援」「被忽略」。這跟前面那條「否定的結論比肯定的結論需要更多樣本」是同一件事的兩個層次：樣本數不足會讓你誤判，**情境選錯則是再多樣本也救不回來**。

這次錯誤的形狀值得單獨記著：**數據本身完全沒問題**——8 次取樣、比率清楚、可重現。錯的是從「這組輸入下的行為」推到「模型的固有屬性」。前兩次（n=1、間接證據）事後都有跡象可循，這次沒有，所以**只能靠事前的習慣攔，不能靠事後檢查發現**。

### 4e. 在替被測系統「編一個機制」之前，先查自己這一側

兩個同型案例（一個是我們的、一個是文檔站的）：

| | 觀測到的異常 | 當下編出來的機制 | 真正的原因 |
|---|---|---|---|
| 我們 | 同一個 budget 有時思考有時不 | 「超過門檻後逐次隨機，約四成」 | 提示詞決定模型要不要思考 |
| 文檔站 | budget 300 回報上限 2457，但 3000 卻成功 | 「上界隨 maxOutputTokens 浮動」 | 他們的正則把錯誤訊息截斷在 46 字元，把 `24576` 切成了 `2457` |

兩次都是**拿異常去給被測系統編一個機制**，而真因都在自己這一側。所以檢查順序要再前置一層：

1. **我的工具沒問題嗎**（解析、截斷、欄位名、判定條件）
2. **我的變因控制住了嗎**（只換了想測的那一個嗎）
3. 才輪到「被測系統有某個我還不知道的機制」

前兩項都在自己手上、驗證成本低，該先查。「上游有個奇怪的行為」是最貴也最容易寫進文檔的假設，要放到最後。

**還有一個維度：當行為本身可能是隨機的，n=1 不是精度不足，是連方向都給不出來。** 這跟一般的取樣誤差不同類——誤差是「數字不準」，這是「結論的形狀就是錯的」。上面那次湊出來的不是「不精確的門檻」而是**一個不存在的門檻**，而且從結果完全看不出異常：128 沒思考、512 有思考，看起來就是一個乾淨漂亮的門檻。

文檔站那邊記了兩個同型的錯誤（他們自己犯的），形狀完全一樣，都是拿單次的「沒看到」去斷言「不支援」：

- **Seedance 1080p**：只送頂層 `size`（doubao 根本不讀），沒生效就據此推翻我們的實測。實際是參數沒送到上游，要送 `metadata.resolution`。
- **Veo**：看到 `/v1/models` 少兩個型號就說文檔寫錯。三個型號其實都能成功呼叫，是列表不完整。

所以**「沒看到」有四種來源，單看結果全都長得像①**：

| | 來源 | 實例 |
|---|---|---|
| ① | 真的不支援 | —— |
| ② | 參數送錯位置，根本沒到上游 | Seedance 1080p（只送頂層 `size`，doubao 不讀） |
| ③ | 支援，但這次沒觸發 | flash-lite 的 `thinkingBudget`（超過門檻仍只有四成） |
| ④ | 查錯地方 | Veo（拿 `/v1/models` 列表當支援度依據，但列表不完整） |

**斷言「不支援」之前的檢查順序**（②和④要先排除，因為它們跑一百次也還是錯的，多次取樣攔不住）：

1. 先排除②——確認參數真的送到上游、而且放在對的層級（找同模型不同路徑的對照組）
2. 再排除④——不要拿列表、metadata、Go struct 這類**間接證據**當行為證據，要看實際呼叫的結果
3. 最後才用多次取樣排除③

順序不能顛倒。先跑一百次去排除③，遇到②或④只會得到一百次一致的錯誤結論。

同一份實測裡有個更適合寫進文檔的發現，順帶記著：`none` 五次全部答錯（8）、其餘四檔五次全部答對（9）——這種**穩定且離散**的差異，n=5 就足以下結論，也比 token 數對使用者更有意義。

### 5. 「參數送了沒作用」幾乎都是閘道沒映射

實例：`glm-5.2` 走 `/v1/messages` 時整個 `thinking` 物件無效——根因是 `service/convert.go` 的 `ClaudeToOpenAIRequest` **只在 OpenRouter 分支讀 `Thinking`**，其餘渠道一律丟棄；`reasoning_effort` 更是連 `dto.ClaudeRequest` 都沒宣告，反序列化階段就消失。

判斷方法：**找一個同模型、不同路徑的對照組**。同一個 `glm-5.2` 在 `/v1/chat/completions` 上 `enable_thinking:false` 能把 token 從 159 降到 19——證明模型做得到，問題在路徑。

**要查的第一個地方是「這個欄位有沒有宣告在對應的 request DTO 裡」。** 沒宣告的欄位會落進 `Extra` map，而那些 DTO 的 `MarshalJSON` **刻意不把 `Extra` 合併回去**，於是欄位在反序列化階段就消失，全程不會有任何錯誤訊息。上面 `reasoning_effort` 沒宣告在 `dto.ClaudeRequest` 就是這個機制。

閘道端 2026-08-12 修掉的一個同型案例（由該 session 同步過來，📄 轉述）：客戶照 Azure 官方文件對 `MAI-Image-2.5` 送 `{"width":1536,"height":864}`，拿到的是 1024×1024 且不報錯——因為 `dto.ImageRequest` 沒有宣告 `width`/`height`，而 `convertToMAIImageRequest` 只讀 `request.Size`。修法是從 `Extra` 取值，並**刻意不宣告在 DTO**（那個結構是所有 image 渠道共用的，一宣告就會把 `width`/`height` 透傳給 dall-e 這類只認 `size` 的上游，把原本能跑的請求變成 400）。

兩個附帶提醒：

- **尺寸字串的分隔符大小寫敏感**。`"1536X864"`（大寫 X）原本解析失敗並靜默退回 1024×1024。本專案已自查：`_MAI_IMAGE_SIZES` 與所有尺寸字串都是小寫 `x` 或 `*`，不受影響。
- **optional 純量欄位要用指標型 + `omitempty`**，否則客戶端顯式送 `0`／`false` 會在 marshal 階段被丟掉（閘道 CLAUDE.md Rule 6；萬相系列的 `prompt_extend` 踩過）。這也是為什麼閘道的 `Priority` 用 `*dto.IntValue`——`0` 是合法值又剛好是預設值。

### 6. UI 不要顯示沒有作用的控制項

這是本專案的一貫原則，`MODELS` 裡不少旗標就是為此存在：

- `thinking`：只有真的能開關思考的模型才給開關
- `no_size`：圖像編輯情境下 `size` 不生效的模型（千問 3.0 已標；**千問 2.0 與萬相編輯也一樣不生效，但尚未處理**）
- `i2v_modes`、`ref_images_only`、`max_ref`、`resolutions`、`reasoning_efforts`：都是「上游只吃這些」的白名單

---

## 五、跨 session 協作紀錄（進行中的事項）

- **`kimi/kimi-k3`（月之暗面直供，走阿里百煉）等待渠道上架**（2026-08-14）。閘道端程式碼已改但在 `feat/carrothub-channel-support` 分支未部署，渠道憑證（華北2／北京地域 API Key、Base URL `https://{WorkspaceId}.cn-beijing.maas.aliyuncs.com`、倍率 1.5 / 5 / 0.1）也還沒設。實查正式環境 `/v1/models`（123 個模型）與 `/api/pricing` **都沒有它**，所以現在加進 `MODELS` 只會做出一個必定失敗、且沒有單價的選項。渠道上線後再上架。
  - ⚠️ **上架時「先不要標 `vision`」**：這個模型的圖片／影片輸入**僅接受公網 URL、不接受 Base64**（上游限制，閘道的 `relay/channel/ali` 是原樣轉發、不做轉存）。而我們平台的視覺輸入送的是 **data URI**（瀏覽器端讀成 base64 塞進 `image_url`），目前的 vision 模型（qwen3-vl、grok-4.3、Gemini）都吃得下所以一直沒事。標了 `vision` 會讓使用者一上傳圖就失敗、而且錯誤訊息看不出根因。要支援得把既有的「上傳到雲端物件儲存換簽名網址」那條路（影片模型在用，因為上游同樣不收 base64）接到文字節點的視覺上傳——那是新功能不是設定。
  - 上架時的設定：`reasoning_efforts: ["max"]`（唯一支援值，也是預設）、**不給思考開關**（純思考模型、關不掉），會回傳 `reasoning_content`。倍率換算與本平台公式一致：輸入 $3/1M ÷ 2 = 1.5、輸出 $15 ÷ $3 = 5。
- **閘道端已修但尚未部署**：`glm-5.2` 的 `thinking` 映射與非串流思考回傳（推到 `feat/carrothub-channel-support`，`main` 未合）。切回 `/v1/messages` 需要：程式碼部署 **＋** 渠道後台開啟「thinking 映射為 enable_thinking」開關（預設關）。目前刻意不切——現在走的 `/v1/chat/completions` 三項能力（開關思考、非串流看得到思考、7 段 `reasoning_effort`）都有，切回去反而少一項。
- **`wan3.0-video`**：已上架但**從未成功呼叫過**，閘道端測試一直是 `AccessDenied`（模型層級未開通）。`input.media` 的 type 詞彙仍未驗證。
- **`glm-5.2-us` / `glm-5.2-fast-preview`**：閘道上沒有，但上架**不需要改程式碼**，後台把模型名加進渠道即可。
- **realtime（即時語音）已通**（2026-08-16，閘道端補完阿里 adaptor 的 realtime 分支）：`qwen3.5-omni-plus-realtime` 對正式環境驗證上架；`qwen3.5-omni-flash-realtime`、`qwen-audio-3.0-realtime-plus`、`qwen-audio-3.0-realtime-flash` 對測試網關（192.168.0.245）驗證上架。**2026-08-17 起三個都已在正式站出現**（閘道部署後 playground 閘門自動放行，pricing 四檔正確）。**尚未做**：①系統公告（`scripts/update_announcements.py`）②通知文檔 session——正等兩件事：文字輸出多收的計費修正部署（見下）、Lyria 渠道綁定，屆時六個一起發（或依 Levi 裁示分開）。快照版 `qwen3.5-omni-flash-realtime-2026-03-15` 經使用者裁示不上架。探測工具：`scripts/probe_realtime.py`。
- **Lyria 三個：正式站 `/api/pricing` 已有（固定價、`enable_groups` 都齊）、`/v1/models` 沒有**（2026-08-17，與閘道端會診定案 📄）：正式 build **含** lyria adaptor（pricing 有固定價＝defaultModelPrice 生效）也含 video_tokens 修正（image 對帳證實 ≥ d512a28），**只差正式後台把三個 lyria 綁進渠道的模型清單**（Levi 做正式操作）。綁完 playground 閘門自動放行、不用改程式碼，屆時跑 lyria 上架驗證。另外：白名單兩本帳修正**從未 commit**（一直在閘道端工作區），任何 build 都沒有→正式站 realtime 文字輸出多收已升級為緊急事項；等「commit＋正式重部署」後閘道端會來叫，再跑白名單對帳（預期 flash 0.0339、plus 0.0571），**在那之前白名單不用再跑**。全部到位後六個模型一起補公告與文檔通知。
- **部署閘門（`app.py` 的 `_DEPLOY_GATED_MODELS`）**：Levi 裁示這批上架是測試性質，正式部署前對外 playground 不可讓使用者選到。六個新模型（realtime 三個＋Lyria 三個）走「上游 `/v1/models` 有才顯示」的動態閘門，正式部署後自動出現。**全部上線後把集合清空**（機制保留給之後的批次）；對外版的 `NENAI_BASE` 維持預設 `https://nen.com.tw`，程式碼沒有寫死任何測試機位址或測試 key。
- **realtime 兩個計費落差：video 已修好（測試機＋正式站都驗畢）、兩本帳修正仍未部署到任何環境**（2026-08-17 對正式站對帳）：正式閘道已部署三個新 realtime 模型（playground 閘門自動放行、pricing 四檔都對），對帳三輪：audio-flash 0.0366、audio-plus 0.0610、omni-flash image 0.0810——video_tokens 計入 ✓、**文字輸出仍被計費 ✗**（三輪都吻合「文字照收」算式）。兩本帳真因（📄 轉述閘道端）：實際扣款路徑 PreWssConsumeQuota 組 QuotaInfo 時漏帶 AudioOnlyOutputBilling 旗標，白名單只在寫日誌那條路生效；修正完成但**未 commit／未部署**。⚠️ **正式站客戶現在被多收文字輸出的錢**（單輪零點幾毫，系統性、與公告費率不符），前端估算（照官方規則）會低於實收。對帳一定要讀扣款側的 `/v1/dashboard/billing/usage`。下一次閘道部署後再驗一輪 basic（預期 flash 0.0339、plus 0.0571）。
- **realtime 聯網搜尋的附加費先前從未入帳**（📄 轉述自閘道端，2026-08-16）：`plugins.search.count` 在逐輪累加時被丟掉，已修未部署。之後若開放 omni-flash 的聯網搜尋 UI（目前未開放、未實測），部署後要對一次搜尋附加費（$10/千次、agent 策略）。
- **MAI 三個 t2i 的 `max_n=1` 是長期鎖，不要解**（2026-08-16 使用者回報＋測試網關重現）：閘道曾對 MAI 的 `n>1` **照 n 張計費、`data[]` 只回 1 張**，多收錢的部分閘道端已修（計費改以上游回報 token 為準）。但（📄 轉述自閘道端）**Azure 上游靜默忽略 `n`、n=3 永遠只回 1 張**——「修好後解鎖」的條件永遠不會發生，`max_n=1`、後端 n 鉗制與 `test_mai_image_n_locked_to_one` 都長期維持。
- **正式環境目前沒有設定雲端物件儲存**（2026-08-11 實測：產出圖片的 `local_path` 是 `/outputs/images/...` 本機路徑）。這造成三件事：影片輸入功能（視頻編輯／動作動畫／影片延伸／r2v 參考影片）不能用（上游不收 base64 影片，一定要雲端網址）、音訊輸入同理、以及產出的圖片在多實例（`maxScale: 5`）或容器重啟後可能 404。要根治就是把 OSS／S3／GCS 任一個的憑證設進 Cloud Run。
- **`/v1/models` 清單有某個模型 ≠ 那條通道可用。** realtime 這次是最清楚的例子：模型在清單裡，但走 WebSocket 會讓閘道 panic。清單只代表「這個 key 的群組被允許使用這個模型名」，不保證任何特定端點或協定能用。新增模型時**先驗端點再做 UI**，不要反過來。
